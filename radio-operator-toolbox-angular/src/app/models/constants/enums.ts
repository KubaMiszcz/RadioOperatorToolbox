export enum TRANSMIT_DIRECTION {
  INCOMING = 'ODBIÓR',
  OUTGOING = 'NADAWANIE',
}

export enum CRUD_METHODS {
  CREATE,
  READ,
  UPDATE,
  DELETE,
}

export enum MODAL_RESULT {
  YES,
  NO,
  CANCEL,
  CLOSE,
}

